---
phase: 03-conversion-layer
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/__mocks__/convex/react.ts
  - src/__tests__/homepage.test.tsx
autonomous: false
requirements:
  - LEAD-01
  - LEAD-02
  - LEAD-03
  - LEAD-04
  - BOOK-01
  - BOOK-02
  - DSGN-02
  - DSGN-03

must_haves:
  truths:
    - "Tests prove form renders all 4 fields"
    - "Tests prove inline validation errors appear for empty required fields"
    - "Tests prove Convex mutation is called with correct args on valid submission"
    - "Tests prove success state renders after submission"
    - "Tests prove booking CTA has target=_blank and correct href"
    - "Tests prove booking CTA is visually primary (orange button classes)"
    - "Visual QA confirms responsive layout at 375px, tablet, and desktop"
  artifacts:
    - path: "src/__mocks__/convex/react.ts"
      provides: "Mock for useMutation in test environment"
      contains: "useMutation"
    - path: "src/__tests__/homepage.test.tsx"
      provides: "Test coverage for LEAD-01..04, BOOK-01..02, DSGN-02..03"
      min_lines: 120
  key_links:
    - from: "src/__tests__/homepage.test.tsx"
      to: "src/components/home/ConversionSection.tsx"
      via: "render ConversionSection or Home page"
      pattern: "ConversionSection|Home"
---

<objective>
Add test coverage for the ConversionSection and run responsive QA across the full homepage.

Purpose: Verify all Phase 3 requirements (LEAD-01..04, BOOK-01..02) are testable and passing, and confirm responsive behavior (DSGN-02, DSGN-03) through visual inspection at mobile/tablet/desktop viewports.

Output: Updated `homepage.test.tsx` with new test cases, `convex/react` mock, and visual QA verification.
</objective>

<execution_context>
@C:/Users/julio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-conversion-layer/03-CONTEXT.md
@.planning/phases/03-conversion-layer/03-RESEARCH.md
@.planning/phases/03-conversion-layer/03-01-SUMMARY.md

@src/__tests__/homepage.test.tsx
@src/components/home/ConversionSection.tsx
@src/app/page.tsx

<interfaces>
<!-- Existing homepage.test.tsx structure -->
```typescript
// Tests for Homepage — covers all 9 Phase 2 requirements
// Uses jest.mock for next/link and lucide-react
// Renders <Home /> in beforeEach
// Tests: HERO-01..03, SRVC-01..02, PRUF-01..03, CTA-01
```

<!-- Existing mocks directory -->
```
src/__mocks__/
  @workos-inc/   (existing)
```

<!-- ConversionSection exports (from plan 01) -->
```typescript
// "use client" component
export function ConversionSection(): JSX.Element
// Uses: useMutation from convex/react, useForm from react-hook-form
// Renders: section#booking with form, booking CTA link
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create convex/react mock and add ConversionSection tests</name>
  <files>src/__mocks__/convex/react.ts, src/__tests__/homepage.test.tsx</files>
  <behavior>
    - LEAD-01: ConversionSection renders input fields for name, email, company, and a textarea for project description
    - LEAD-02: Submitting the form with empty required fields shows inline error messages (name, email, message required)
    - LEAD-03: Filling all required fields and submitting calls the Convex mutation mock with { name, email, message, source: "homepage" }
    - LEAD-04: After successful submission, form is replaced by success message containing "We'll be in touch"
    - BOOK-01: Booking CTA link has target="_blank" and rel containing "noopener"
    - BOOK-02: Booking CTA has orange/primary button styling (bg-cta class or similar); form submit button has ghost/outline styling
  </behavior>
  <action>
**Step 1: Create `src/__mocks__/convex/react.ts`**

```typescript
export const useMutation = jest.fn(() => jest.fn());
export const useQuery = jest.fn(() => undefined);
export const useConvex = jest.fn();
```

This mock intercepts all `useMutation` calls so ConversionSection can render without a Convex provider.

**Step 2: Update `src/__tests__/homepage.test.tsx`**

Add the following to the existing test file:

1. Add `convex/react` mock at the top (after existing mocks):
```typescript
const mockSubmitLead = jest.fn();
jest.mock("convex/react", () => ({
  useMutation: jest.fn(() => mockSubmitLead),
  useQuery: jest.fn(() => undefined),
}));
```

2. Also mock `../../../../convex/_generated/api` (the relative import used by ConversionSection):
```typescript
jest.mock("../../../../convex/_generated/api", () => ({
  api: { leads: { submit: "leads:submit" } },
}));
```
Note: Check the actual import path used in ConversionSection.tsx and match it exactly.

3. Add `CheckCircle` to the lucide-react mock icons.

4. Add import for `userEvent` from `@testing-library/user-event` and `waitFor` from `@testing-library/react`.

5. Add a new `describe("ConversionSection")` block with these tests:

- **LEAD-01:** Render Home, verify 4 form fields exist (getByPlaceholderText or getByRole for name, email, company, textarea)
- **LEAD-02:** Render Home, click submit without filling fields, use `waitFor` to assert error messages appear ("Name is required", "Email is required", "Please describe your project")
- **LEAD-03:** Render Home, fill name/email/message fields with userEvent.type, submit form, assert `mockSubmitLead` was called with `expect.objectContaining({ name: "Test User", email: "test@example.com", message: "Test project", source: "homepage" })`
- **LEAD-04:** Render Home, fill and submit form (mock resolves successfully), use `waitFor` to assert success message "We'll be in touch" appears
- **BOOK-01:** Find the booking CTA link (by role "link" with name matching "Book" or "Consultation"), assert `target="_blank"` and `rel` contains "noopener"
- **BOOK-02:** Find the booking CTA link, verify it has primary/orange styling class (check for `bg-cta` or `bg-primary` in className); find the form submit button, verify it does NOT have `bg-cta` class (ghost/outline style)

6. Reset `mockSubmitLead` in `beforeEach` within the ConversionSection describe block.

**Important:** The existing Phase 2 tests in the "Homepage" describe block must continue to pass unchanged. Do NOT modify the existing test cases — only add new ones.
  </action>
  <verify>
    <automated>cd C:/01_repos/sira-services && npx jest --testPathPattern=homepage --verbose 2>&1 | tail -40</automated>
  </verify>
  <done>All existing Phase 2 tests pass. All new Phase 3 tests (LEAD-01..04, BOOK-01, BOOK-02) pass. The convex/react mock exists and correctly stubs useMutation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Responsive QA across full homepage</name>
  <action>Human verifies the complete conversion layer and responsive behavior across all viewports. All automated tests are already passing from Task 1.</action>
  <what-built>Complete conversion layer: booking CTA + lead form with validation, success/error states, and responsive layout. All automated tests passing.</what-built>
  <how-to-verify>
    1. Run `npm run dev:frontend` and open http://localhost:3000 in your browser
    2. **Desktop (full width):**
       - Scroll to the booking section (or click "Book a Consultation" in the hero)
       - Verify two-column layout: booking CTA card on left, lead form on right
       - Verify booking CTA button is orange/filled and visually dominant
       - Verify form submit button is ghost/outline style (clearly secondary)
       - Verify "or" divider between the two columns
    3. **Form validation:**
       - Click submit with empty fields — verify inline error messages appear below name, email, and project description fields
       - Enter an invalid email (e.g., "notanemail") and submit — verify email-specific error
       - Fill all required fields and submit — verify success message replaces the form
    4. **Booking CTA:**
       - Click the booking button — verify it opens in a new tab (placeholder URL is fine for now)
    5. **Mobile (375px):**
       - Open DevTools, set viewport to 375px width
       - Verify booking card stacks on top, form below
       - Verify horizontal "or" divider between stacked cards
       - Verify all inputs are full-width
       - Verify no horizontal scroll
       - Verify all sections above (hero, services, testimonials, CTA banner) look correct at 375px
    6. **Tablet (~768px):**
       - Set viewport to 768px
       - Verify layout transitions appropriately between mobile and desktop
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern=homepage` passes all tests (Phase 2 + Phase 3)
- Visual QA confirms responsive layout at 375px, 768px, and desktop
- No horizontal scroll at any viewport
- All interactive elements have adequate touch targets
</verification>

<success_criteria>
- All LEAD-01..04 tests pass: form fields render, validation works, mutation called, success state shown
- All BOOK-01..02 tests pass: CTA has target="_blank", CTA is visually primary
- DSGN-02/DSGN-03 confirmed via human visual QA at 375px mobile, tablet, and desktop
- Full test suite (`npm test`) passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-conversion-layer/03-02-SUMMARY.md`
</output>
