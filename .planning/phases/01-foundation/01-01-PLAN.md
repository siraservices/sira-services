---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tailwind.config.ts
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/Navigation.tsx
  - src/components/Footer.tsx
  - src/app/about/page.tsx
  - src/app/services/page.tsx
  - src/app/signin/page.tsx
  - src/app/signup/page.tsx
  - src/__tests__/navigation-auth.test.tsx
  - src/__tests__/signin-page.test.tsx
  - src/__tests__/signup-page.test.tsx
  - src/components/home/.gitkeep
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - DSGN-01

must_haves:
  truths:
    - "Page body renders dark background (#333e48) with no flash of light content on hard refresh"
    - "Tailwind config contains #ff9900 as the primary accent token"
    - "Navigation shows only Home, Blog, Contact links (no Services, About, Sign In)"
    - "Footer shows only Blog, Contact links (no Services, About)"
    - "Libraries motion, zod, @hookform/resolvers, react-calendly are installed and importable"
    - "src/components/home/ directory exists"
    - "Deleted pages (about, services, signin, signup) return 404"
  artifacts:
    - path: "tailwind.config.ts"
      provides: "Dark palette tokens (primary, surface, text, cta)"
      contains: "#ff9900"
    - path: "src/app/globals.css"
      provides: "Updated custom utilities for dark palette"
      contains: "rgba(255, 153, 0"
    - path: "src/app/layout.tsx"
      provides: "Dark body class for FOUC prevention"
      contains: "bg-surface"
    - path: "src/components/Navigation.tsx"
      provides: "Pruned nav links and dark styling"
    - path: "src/components/Footer.tsx"
      provides: "Pruned footer links and dark styling"
    - path: "src/components/home/.gitkeep"
      provides: "Home component directory scaffold"
  key_links:
    - from: "tailwind.config.ts"
      to: "src/app/layout.tsx"
      via: "bg-surface token resolves to #333e48"
      pattern: "bg-surface"
    - from: "tailwind.config.ts"
      to: "src/app/globals.css"
      via: "@apply directives reference new token names"
      pattern: "text-text|text-primary|bg-surface"
    - from: "tailwind.config.ts"
      to: "src/components/Navigation.tsx"
      via: "Tailwind classes reference new tokens"
      pattern: "text-text|bg-surface"
---

<objective>
Establish the dark design foundation for the Sira Services homepage by replacing all light-mode Tailwind tokens with the locked dark palette, updating global CSS utilities, preventing FOUC with server-rendered dark body class, cleaning up unused pages, and installing Phase 2+ libraries.

Purpose: Every subsequent component in Phases 2-3 can be built without revisiting global configuration. The site renders dark from first paint.
Output: Updated tailwind.config.ts, globals.css, layout.tsx, Navigation.tsx, Footer.tsx; deleted pages; installed libraries; home component directory.
</objective>

<execution_context>
@C:/Users/julio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

@tailwind.config.ts
@src/app/globals.css
@src/app/layout.tsx
@src/components/Navigation.tsx
@src/components/Footer.tsx
@src/components/ui/liquid-glass-button.tsx
@src/__tests__/navigation-auth.test.tsx

<interfaces>
<!-- Current token structure being replaced. Executor needs to see the full current config to replace correctly. -->

From tailwind.config.ts (current colors - REPLACE ALL):
```typescript
colors: {
  primary: { DEFAULT: "#000000", light: "#333333", dark: "#000000", 50-900: "..." },
  cta: { DEFAULT: "#FFD700", light: "#FFE033", dark: "#FF8C00" },
  surface: { DEFAULT: "#FFFFFF", muted: "#FFF8E7", border: "#D4CFC4", hover: "#FFF0C8" },
  text: { DEFAULT: "#1A1A1A", muted: "#666666", dim: "#999999" },
}
```

From layout.tsx (current body class - UPDATE):
```tsx
<body className="font-body antialiased bg-surface-muted text-text">
```

From Navigation.tsx (current navLinks - PRUNE):
```typescript
const navLinks = [
  { href: "/", label: "Home" },
  { href: "/services", label: "Services" },
  { href: "/blog", label: "Blog" },
  { href: "/about", label: "About" },
  { href: "/contact", label: "Contact" },
];
```

From Footer.tsx (current links - PRUNE):
```typescript
[
  { href: "/services", label: "Services" },
  { href: "/blog", label: "Blog" },
  { href: "/about", label: "About" },
  { href: "/contact", label: "Contact" },
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Tailwind tokens with dark palette and update global styles</name>
  <files>tailwind.config.ts, src/app/globals.css, src/app/layout.tsx</files>
  <action>
**tailwind.config.ts** — Replace the entire `theme.extend.colors` block with the locked dark palette tokens:

```typescript
colors: {
  primary: {
    DEFAULT: "#ff9900",
    dark: "#cc7a00",
    light: "#ffad33",
  },
  surface: {
    DEFAULT: "#333e48",
    alt: "#2a323a",
    nav: "#2a323a",
  },
  text: {
    DEFAULT: "#ffffff",
    body: "#e0e4e8",
    muted: "#a0aab4",
  },
  cta: {
    DEFAULT: "#ff9900",
    text: "#1a1a1a",
  },
},
```

Remove the entire primary 50-900 scale, surface-border, surface-hover, surface-muted, text-dim tokens. Keep all other theme.extend blocks (fontFamily, animation, keyframes, boxShadow) unchanged. Update the `cta-glow` boxShadow from gold to orange: `"0 4px 14px rgba(255, 153, 0, 0.35)"`.

**src/app/globals.css** — Update all custom utilities referencing old palette colors:

1. `.text-gradient`: Change to `linear-gradient(135deg, #ff9900 0%, #ffad33 100%)` (keep gradient text per Claude's discretion — orange gradient on dark reads as premium/tech).

2. Scrollbar: track `#2a323a`, thumb `#a0aab4`, thumb:hover `#e0e4e8`.

3. `::selection`: `background: rgba(255, 153, 0, 0.25); color: #ffffff;`

4. Prose styles — update all `@apply` token references:
   - `.prose h1/h2/h3`: `text-text` stays (now resolves to #ffffff)
   - `.prose p/ul/ol`: change `text-text-muted` to `text-text-body`
   - `.prose code`: change `bg-primary-50 text-primary` to `bg-surface-alt text-primary` (primary now orange, bg is dark surface)
   - `.prose blockquote`: change `border-cta` to `border-primary`
   - `.prose a`: change `text-cta-dark hover:text-cta` to `text-primary hover:text-primary-light`
   - `.prose strong`: `text-text` stays (now white)

**src/app/layout.tsx** — Change body class from `bg-surface-muted text-text` to `bg-surface text-text-body`. This renders #333e48 background server-side, preventing FOUC. Do NOT use slate-950 (too dark) or any `dark:` prefix.
  </action>
  <verify>
    <automated>cd C:/01_repos/sira-services && npx tailwindcss --content "src/app/layout.tsx" --config tailwind.config.ts 2>&1 | head -5 && node -e "const c = require('./tailwind.config.ts'); console.log('SKIP - TS config')" 2>/dev/null; echo "Check: grep for old tokens" && grep -rn "FFD700\|FFF8E7\|surface-muted\|text-dim\|primary-50\|cta-dark\|cta-light" tailwind.config.ts src/app/globals.css src/app/layout.tsx; echo "Exit: $?"</automated>
  </verify>
  <done>tailwind.config.ts has #ff9900 as primary.DEFAULT and #333e48 as surface.DEFAULT. globals.css has no references to old gold/light palette hex values. layout.tsx body class is "font-body antialiased bg-surface text-text-body". No old token names (surface-muted, text-dim, primary-50, cta-dark, cta-light) remain in these three files.</done>
</task>

<task type="auto">
  <name>Task 2: Delete unused pages, update Nav/Footer for dark theme, install libraries, scaffold home directory, update tests</name>
  <files>src/components/Navigation.tsx, src/components/Footer.tsx, src/app/about/page.tsx, src/app/services/page.tsx, src/app/signin/page.tsx, src/app/signup/page.tsx, src/__tests__/navigation-auth.test.tsx, src/__tests__/signin-page.test.tsx, src/__tests__/signup-page.test.tsx, src/components/home/.gitkeep, package.json, package-lock.json</files>
  <action>
**Delete unused pages** — Remove these four directories entirely:
- `src/app/about/` (entire directory)
- `src/app/services/` (entire directory)
- `src/app/signin/` (entire directory)
- `src/app/signup/` (entire directory)

Before deleting, verify no kept page imports from these directories. The grep shows only test files (`src/__tests__/signin-page.test.tsx`, `src/__tests__/signup-page.test.tsx`) import from them — those test files should also be deleted.

**Delete stale test files:**
- `src/__tests__/signin-page.test.tsx`
- `src/__tests__/signup-page.test.tsx`

**src/components/Navigation.tsx** — Three changes:

1. Prune navLinks array to:
```typescript
const navLinks = [
  { href: "/", label: "Home" },
  { href: "/blog", label: "Blog" },
  { href: "/contact", label: "Contact" },
];
```

2. Remove the Sign In button entirely (both desktop and mobile). The `<Link href="/signin">` wrapping the LiquidButton in the desktop section AND the mobile `<Link href="/signin">` section should both be removed. Keep the authenticated user display (name + sign out button) — admin users can still reach `/admin` and WorkOS middleware handles auth. Remove the `LogIn` import from lucide-react since it is no longer used.

3. Restyle for dark theme:
   - Header background: change `bg-white/90` (scrolled) and `bg-white/60` (unscrolled) to `bg-surface-nav/90` and `bg-surface-nav/60`
   - Remove `border-surface-border` references (token removed) — use `border-text-muted/20` or similar for subtle borders
   - Active link style: change `text-primary bg-primary-50` to `text-primary bg-primary/10`
   - Inactive link style: change `text-text-muted hover:text-text hover:bg-surface-hover` to `text-text-muted hover:text-text hover:bg-surface-alt`
   - Logo "SIRA" text: change `text-primary-dark` to `text-text` (white)
   - Logo ".services" text: `text-cta` stays (now resolves to orange)
   - Mobile menu border: replace `border-surface-border` with `border-text-muted/20`
   - Mobile menu items: same active/inactive treatment as desktop

**src/components/Footer.tsx** — Two changes:

1. Prune navigation links to:
```typescript
[
  { href: "/blog", label: "Blog" },
  { href: "/contact", label: "Contact" },
]
```

2. Restyle for dark theme:
   - Footer background: change `bg-primary-900` to `bg-surface-alt`
   - Description text: change `text-primary-300` to `text-text-muted`
   - Link text: change `text-primary-300 hover:text-white` to `text-text-muted hover:text-text`
   - Social icon buttons: change `bg-primary-800 text-primary-300 hover:text-white hover:bg-primary-700` to `bg-surface text-text-muted hover:text-text hover:bg-surface`
   - Bottom border: change `border-primary-800` to `border-text-muted/20`
   - Copyright text: change `text-primary-400` to `text-text-muted`
   - Logo ".services" text: change `text-cta-light` to `text-cta` (cta-light removed)

**Install libraries:**
```bash
npm install motion react-calendly zod @hookform/resolvers
```

**Scaffold home component directory:**
```bash
mkdir -p src/components/home && touch src/components/home/.gitkeep
```

**Update navigation test** (`src/__tests__/navigation-auth.test.tsx`):
- In the "renders all navigation links" test (around line 140-154): Remove assertions for "Services" and "About". Keep assertions for "Home", "Blog", "Contact".
- In the "shows Sign In link when user is not authenticated" test: The Sign In button is removed from Nav, so this test should be updated to assert that Sign In is NOT rendered when unauthenticated (or remove the test). Since the sign-in link no longer exists in the component, update the test to verify that when user is null and loading is false, NO sign-in link is rendered (the auth section only shows content for authenticated users now).
- Update the test at line 64-66 to instead assert: `expect(screen.queryByText("Sign In")).not.toBeInTheDocument()` when user is null.
  </action>
  <verify>
    <automated>cd C:/01_repos/sira-services && echo "=== Deleted pages ===" && (test ! -d src/app/about && test ! -d src/app/services && test ! -d src/app/signin && test ! -d src/app/signup && echo "PASS: pages deleted") || echo "FAIL: pages still exist" && echo "=== Home dir ===" && (test -d src/components/home && echo "PASS: home dir exists") || echo "FAIL: home dir missing" && echo "=== Libraries ===" && node -e "require('motion'); require('zod'); require('@hookform/resolvers'); require('react-calendly'); console.log('PASS: all libs importable')" 2>&1 && echo "=== Nav links ===" && grep -c "Services\|About" src/components/Navigation.tsx; echo "=== Tests ===" && npx jest --testPathPattern=navigation-auth --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <done>Four page directories deleted. Stale test files deleted. Navigation shows only Home, Blog, Contact with no Sign In button. Footer shows only Blog, Contact. Both Nav and Footer use dark palette tokens (no references to old primary-300/400/700/800/900, surface-border, surface-hover, primary-50 tokens). Libraries installed. Home component directory exists. Navigation auth test passes with updated assertions.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without TypeScript errors (confirms no broken imports from deleted pages, all token references valid)
2. `npx jest --no-coverage` — all tests pass (navigation-auth updated, stale signin/signup tests removed)
3. Visual check: `npm run dev:frontend` — page renders dark background immediately on hard refresh in incognito (no white flash)
4. No references to old palette hex values (#FFD700, #FFF8E7, #000000 as primary) in tailwind.config.ts, globals.css, layout.tsx, Navigation.tsx, Footer.tsx
</verification>

<success_criteria>
- Page renders #333e48 dark background from first paint (no FOUC)
- Tailwind config primary token is #ff9900 (orange), no gold/blue tokens remain
- Navigation: Home, Blog, Contact only — no Services, About, Sign In links
- Footer: Blog, Contact only — no Services, About links
- Both Nav and Footer styled with dark palette (no light-mode token references)
- `npm run build` succeeds
- All tests pass
- motion, zod, @hookform/resolvers, react-calendly importable
- src/components/home/ directory exists
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
