---
phase: 02-content-sections
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/__tests__/homepage.test.tsx
  - src/app/page.tsx
autonomous: true
requirements: [HERO-01, HERO-02, HERO-03, SRVC-01, SRVC-02, PRUF-01, PRUF-02, PRUF-03, CTA-01]

must_haves:
  truths:
    - "Homepage renders all 4 sections in correct order: Hero, Services, Testimonials, CTA Banner"
    - "A #booking placeholder section exists at the bottom for Phase 3 scroll target"
    - "page.tsx is a thin composition root with no business logic or data"
    - "All 9 Phase 2 requirement behaviors are verified by automated tests"
    - "No light-mode classes survive from the old page.tsx"
  artifacts:
    - path: "src/__tests__/homepage.test.tsx"
      provides: "Unit tests for all Phase 2 requirements"
      min_lines: 50
    - path: "src/app/page.tsx"
      provides: "Thin composition root importing 4 section components"
      max_lines: 30
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/home/HeroSection.tsx"
      via: "import and render"
      pattern: "import.*HeroSection.*from"
    - from: "src/app/page.tsx"
      to: "src/components/home/ServicesSection.tsx"
      via: "import and render"
      pattern: "import.*ServicesSection.*from"
    - from: "src/app/page.tsx"
      to: "src/components/home/TestimonialsSection.tsx"
      via: "import and render"
      pattern: "import.*TestimonialsSection.*from"
    - from: "src/app/page.tsx"
      to: "src/components/home/CtaBanner.tsx"
      via: "import and render"
      pattern: "import.*CtaBanner.*from"
---

<objective>
Wire the homepage test scaffold and rewrite page.tsx as a thin composition root that renders the four section components from Plan 01.

Purpose: Validates all 9 Phase 2 requirements via automated tests, and replaces the entire old light-mode page.tsx with a clean dark-themed composition of section components.

Output: A passing test suite covering HERO-01 through CTA-01, and a rewritten page.tsx that composes the homepage.
</objective>

<execution_context>
@C:/Users/julio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@.planning/phases/02-content-sections/02-01-SUMMARY.md

@src/__tests__/navigation-auth.test.tsx (mock patterns for next/link, lucide-react)

<interfaces>
<!-- Components created by Plan 01 (02-01) -->
From src/components/home/HeroSection.tsx:
  export function HeroSection(): JSX.Element
  - Renders hero with h1 headline, p subtext, a[href="#booking"] CTA

From src/components/home/ServicesSection.tsx:
  export function ServicesSection(): JSX.Element
  - Renders 3 service cards with titles: "AI Consulting & Strategy", "Data Pipeline Implementation", "Custom Computer Vision Systems"

From src/components/home/TestimonialsSection.tsx:
  export function TestimonialsSection(): JSX.Element
  - Renders 3 testimonials with exact verbatim text
  - Jesse Batt: "...Would 100% recommend"
  - Kerry Johnson: "...punctual and eager to learn."
  - Daniel: "...qualifying questions...dial in on the analysis..."

From src/components/home/CtaBanner.tsx:
  export function CtaBanner(): JSX.Element
  - Renders CTA banner with a[href="#booking"] link
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create homepage test scaffold</name>
  <files>src/__tests__/homepage.test.tsx</files>
  <behavior>
    - HERO-01: page renders an h1 heading element (outcome-focused headline exists)
    - HERO-02: page renders a paragraph with supporting subtext (check for key phrase like "business" or "consultation" or "AI")
    - HERO-03: page renders a link/anchor with text "Book a Consultation" and href containing "#booking"
    - SRVC-01: page renders exactly 3 service card titles: "AI Consulting & Strategy", "Data Pipeline Implementation", "Custom Computer Vision Systems"
    - SRVC-02: page renders description text for each service (getByText or queryAllByRole checks)
    - PRUF-01: page renders text containing "100% recommend" (Jesse Batt quote)
    - PRUF-02: page renders text containing "punctual and eager to learn" (Kerry Johnson quote)
    - PRUF-03: page renders text containing "qualifying questions" (Daniel quote)
    - CTA-01: page renders at least 2 elements with href="#booking" (Hero CTA + CTA banner)
  </behavior>
  <action>
Create `src/__tests__/homepage.test.tsx` following the mock pattern from `navigation-auth.test.tsx`.

Mocks needed:
- `next/link` -- mock as `<a>` tag (same pattern as navigation-auth.test.tsx)
- `lucide-react` -- mock Brain, Database, Eye (and any other icons used) as simple spans with data-testid

Import the page component: `import Home from "@/app/page"` (default export).

Write a describe block "Homepage" with individual test cases for each behavior listed above. Use `render(<Home />)` and then `screen.getByText`, `screen.getByRole`, `screen.getAllByRole` to assert.

For the "exactly 3 service titles" test: use `screen.getByText("AI Consulting & Strategy")`, `screen.getByText("Data Pipeline Implementation")`, `screen.getByText("Custom Computer Vision Systems")` -- all must be present.

For testimonial tests: use `screen.getByText` with a regex or substring matcher -- e.g., `screen.getByText(/100% recommend/)`.

For CTA href tests: use `screen.getAllByRole("link", { name: /book a consultation/i })` and check `.length >= 2` and each has `href` attribute containing `#booking`.

Run tests FIRST -- they should FAIL (RED) since page.tsx has not been rewritten yet. This is expected.
  </action>
  <verify>
    <automated>npm run test -- --testPathPattern=homepage --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists with 9+ test cases covering all Phase 2 requirements. Tests run (they will fail at this point since page.tsx is not yet rewritten -- that is correct RED phase behavior).</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite page.tsx as thin composition root</name>
  <files>src/app/page.tsx</files>
  <action>
FULLY REPLACE `src/app/page.tsx` (the entire file, not incremental edits). The old file is 214 lines of light-mode code that is structurally incompatible.

New page.tsx should be approximately 20 lines:

```tsx
import { HeroSection } from "@/components/home/HeroSection";
import { ServicesSection } from "@/components/home/ServicesSection";
import { TestimonialsSection } from "@/components/home/TestimonialsSection";
import { CtaBanner } from "@/components/home/CtaBanner";

export default function Home() {
  return (
    <div>
      <HeroSection />
      <ServicesSection />
      <TestimonialsSection />
      <CtaBanner />
      {/* Phase 3 scroll target for #booking anchor */}
      <section id="booking" className="bg-surface" />
    </div>
  );
}
```

Key points:
- NO "use client" directive (Server Component)
- NO data fetching, no state, no logic -- pure composition
- Import all 4 section components from @/components/home/
- Include `<section id="booking" className="bg-surface" />` as Phase 3 placeholder scroll target
- Do NOT import lucide-react, LiquidButton, Link, or any other dependency -- those live in the section components
- Do NOT carry forward ANY old page.tsx content: no blur orbs, no gradient backgrounds, no metrics bar, no old services array, no /contact links, no /services links, no /about links
- Remove the .gitkeep file from src/components/home/ if it still exists

After rewriting, run the full test suite. All homepage tests from Task 1 should now PASS (GREEN).
  </action>
  <verify>
    <automated>npm run test -- --no-coverage</automated>
  </verify>
  <done>page.tsx is under 30 lines. Imports and renders all 4 section components. Has #booking placeholder. All homepage tests pass. Full test suite (including navigation-auth, middleware, auth-actions) still passes. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- `npm run test -- --no-coverage` passes all tests (existing + new homepage tests)
- `npx tsc --noEmit` passes with no errors
- page.tsx contains no light-mode classes (no bg-white, bg-gradient, bg-[#FFF, text-[#1A1A1A], blur-[120px])
- page.tsx is under 30 lines
- Homepage tests cover all 9 requirement IDs
</verification>

<success_criteria>
The homepage test suite verifies all 9 Phase 2 requirements. page.tsx is a thin composition root under 30 lines. All tests pass. The old light-mode homepage is fully replaced with the dark-themed section composition.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-02-SUMMARY.md`
</output>
