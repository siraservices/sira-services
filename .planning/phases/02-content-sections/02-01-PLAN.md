---
phase: 02-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/HeroSection.tsx
  - src/components/home/ServicesSection.tsx
  - src/components/home/TestimonialsSection.tsx
  - src/components/home/CtaBanner.tsx
autonomous: true
requirements: [HERO-01, HERO-02, HERO-03, SRVC-01, SRVC-02, PRUF-01, PRUF-02, PRUF-03, CTA-01]

must_haves:
  truths:
    - "Hero section renders an outcome-focused headline and supporting subtext"
    - "Hero CTA button says 'Book a Consultation' and links to #booking"
    - "Services grid renders exactly 3 cards with correct titles: AI Consulting & Strategy, Data Pipeline Implementation, Custom Computer Vision Systems"
    - "Each service card has a lucide-react icon, title, and benefit-oriented description"
    - "Three testimonials render with exact verbatim text from requirements"
    - "Jesse Batt testimonial includes role 'Owner of Performance Meal Prep'"
    - "Kerry Johnson and Daniel testimonials have no role subtitle"
    - "CTA banner renders with a booking link"
  artifacts:
    - path: "src/components/home/HeroSection.tsx"
      provides: "Hero section with headline, subtext, CTA"
      min_lines: 25
    - path: "src/components/home/ServicesSection.tsx"
      provides: "3-card services grid"
      min_lines: 30
    - path: "src/components/home/TestimonialsSection.tsx"
      provides: "3 testimonial cards with exact quote text"
      min_lines: 40
    - path: "src/components/home/CtaBanner.tsx"
      provides: "Bottom CTA banner with booking link"
      min_lines: 15
  key_links:
    - from: "src/components/home/HeroSection.tsx"
      to: "#booking"
      via: "anchor href on CTA button"
      pattern: 'href="#booking"'
    - from: "src/components/home/CtaBanner.tsx"
      to: "#booking"
      via: "anchor href on CTA link"
      pattern: 'href="#booking"'
---

<objective>
Create the four homepage section components: HeroSection, ServicesSection, TestimonialsSection, and CtaBanner.

Purpose: These are the content building blocks that tell the Sira Services story -- who they are, what they offer, why to trust them, and how to engage. All are React Server Components with static data arrays and dark palette tokens from Phase 1.

Output: Four `.tsx` files in `src/components/home/` ready to be composed into `page.tsx` in Plan 02.
</objective>

<execution_context>
@C:/Users/julio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

@src/app/page.tsx (current light-mode homepage -- REPLACE in Plan 02, reference only for structure)
@tailwind.config.ts (dark palette tokens)
@src/lib/utils.ts (cn() utility)

<interfaces>
<!-- Dark palette tokens from tailwind.config.ts (Phase 1) -->
Token reference:
- Headings: text-text (#ffffff)
- Body copy: text-text-body (#e0e4e8)
- Muted/labels: text-text-muted (#a0aab4)
- Accent/icons: text-primary (#ff9900)
- CTA buttons: bg-cta text-cta-text (orange bg, dark text)
- Main bg: bg-surface (#333e48)
- Alternate bg: bg-surface-alt (#2a323a)
- Card shadows: shadow-card, shadow-elevated
- CTA glow: shadow-cta-glow (hover effect)
- Font display: font-display (headings)
- Font body: font-body (paragraphs)

Section background alternation (locked decision):
- Hero: bg-surface
- Services: bg-surface-alt
- Testimonials: bg-surface
- CTA Banner: bg-surface-alt

cn() utility available at @/lib/utils
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeroSection and ServicesSection components</name>
  <files>src/components/home/HeroSection.tsx, src/components/home/ServicesSection.tsx</files>
  <action>
Create two React Server Components (NO "use client" directive) in src/components/home/.

**HeroSection.tsx** (HERO-01, HERO-02, HERO-03):
- Section wrapper: `<section className="py-24 px-6 bg-surface">` (or min-h-[80vh] flex items-center for visual impact)
- Inner container: `<div className="max-w-7xl mx-auto">`
- Headline (h1): outcome-focused, communicates what business problems Sira Services solves. Use text-text, font-display, font-bold, text-5xl md:text-7xl. Example direction: "Build AI Systems That Drive Real Business Results" or similar. Do NOT reuse the old "Trusted expertise in AI & Machine Learning" (feature-focused).
- Apply `text-gradient` class (from globals.css) to a key phrase for accent.
- Subtext (p): explains who the service is for and value delivered. Use text-text-body, font-body, text-lg md:text-xl, max-w-xl.
- CTA button: `<a href="#booking">` (native anchor, not next/link -- scroll target). Text: "Book a Consultation". Styled: `inline-flex items-center justify-center gap-2 bg-cta text-cta-text font-display font-semibold px-8 py-4 rounded-lg hover:-translate-y-1 hover:shadow-cta-glow transition-all duration-200 cursor-pointer`.
- Do NOT include: blur orbs, grid patterns, opacity-0 animate-fade-up classes, links to /contact or /services (deleted pages), LiquidButton import, motion animations (v2 scope).
- Do NOT use any hardcoded hex colors -- use tokens only.

**ServicesSection.tsx** (SRVC-01, SRVC-02):
- Section wrapper: `<section className="py-24 px-6 bg-surface-alt">`
- Inner container: `<div className="max-w-7xl mx-auto">`
- Section heading: "How We Help You Succeed" or similar, centered. Use text-text, font-display, text-3xl md:text-4xl.
- Section subtext: brief line about end-to-end AI solutions. Use text-text-muted.
- Define a `services` const array with EXACTLY 3 items (not 4):
  1. `{ icon: Brain, title: "AI Consulting & Strategy", description: "..." }` -- benefit-oriented description about helping businesses create actionable AI roadmaps
  2. `{ icon: Database, title: "Data Pipeline Implementation", description: "..." }` -- benefit-oriented about turning raw data into reliable automated workflows
  3. `{ icon: Eye, title: "Custom Computer Vision Systems", description: "..." }` -- benefit-oriented about automating visual inspection and analysis
- Import `{ Brain, Database, Eye }` from "lucide-react".
- Grid: `grid md:grid-cols-3 gap-6` (3 columns, not 4).
- Card pattern: `group relative p-7 rounded-xl bg-surface border border-text-muted/20 shadow-card hover:shadow-elevated hover:-translate-y-1 transition-all duration-200 cursor-pointer`
- Icon container: `p-3 rounded-xl bg-primary/10 w-fit mb-5 group-hover:bg-primary transition-colors duration-200`
- Icon: `h-6 w-6 text-primary group-hover:text-cta-text transition-colors duration-200`
- Title: font-display font-semibold text-text text-lg
- Description: text-sm text-text-body leading-relaxed font-body
- Do NOT include "Learn more" hover text (no destination page exists).
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/home/HeroSection.tsx src/components/home/ServicesSection.tsx 2>&1 || echo "TypeScript check done"</automated>
  </verify>
  <done>HeroSection.tsx exports HeroSection with outcome-focused headline, subtext, and #booking CTA. ServicesSection.tsx exports ServicesSection with exactly 3 cards matching SRVC-01 titles. Both are Server Components (no "use client"). No hardcoded hex colors. No links to deleted pages.</done>
</task>

<task type="auto">
  <name>Task 2: Create TestimonialsSection and CtaBanner components</name>
  <files>src/components/home/TestimonialsSection.tsx, src/components/home/CtaBanner.tsx</files>
  <action>
Create two React Server Components (NO "use client" directive) in src/components/home/.

**TestimonialsSection.tsx** (PRUF-01, PRUF-02, PRUF-03):
- Section wrapper: `<section className="py-24 px-6 bg-surface">`
- Inner container: `<div className="max-w-7xl mx-auto">`
- Section heading: "What Our Clients Say" or similar, centered. text-text, font-display, text-3xl md:text-4xl.
- Define a `testimonials` const array with EXACTLY this data (copy verbatim -- do NOT paraphrase or truncate):
  ```
  [
    {
      name: "Jesse Batt",
      role: "Owner of Performance Meal Prep",
      quote: "Amazing experience! Super fast at addressing issues. Always making suggestions to better our site. Would 100% recommend",
    },
    {
      name: "Kerry Johnson",
      role: null,
      quote: "Julio, is a young vibrant individual who enjoys his work. Great communication, punctual and eager to learn.",
    },
    {
      name: "Daniel",
      role: null,
      quote: "Julio was very polite and professional. He asked great qualifying questions and we were able to dial in on the analysis that suited the project best. I would recommend his services and look forward to working with him in the future.",
    },
  ]
  ```
- CRITICAL: The quote text must be EXACTLY as shown above -- character for character. "100% recommend" not "100 percent". "Julio, is" (with comma). "dial in on the analysis" not "dial in the analysis".
- Grid: `grid md:grid-cols-3 gap-6`
- Card pattern: `p-6 rounded-xl bg-surface-alt border border-text-muted/20 flex flex-col gap-4`
- Quote text: Use `&ldquo;` and `&rdquo;` around the quote. text-text-body leading-relaxed font-body italic.
- Attribution: name in font-display font-semibold text-text text-sm. Role (if not null) in text-text-muted text-xs mt-0.5. Conditionally render role -- do NOT render an empty element for Kerry Johnson or Daniel.
- Make `role` optional in the type (use `role: string | null`).

**CtaBanner.tsx** (CTA-01):
- Section wrapper: `<section className="py-24 px-6 bg-surface-alt">`
- Inner container: `<div className="max-w-3xl mx-auto text-center">`
- Heading: "Ready to Transform Your Business with AI?" or similar. text-text, font-display, text-3xl md:text-5xl.
- Subtext: brief encouragement to book. text-text-muted, font-body.
- CTA link: `<a href="#booking">` with text "Book a Consultation". Same CTA button style as HeroSection: `inline-flex items-center justify-center gap-2 bg-cta text-cta-text font-display font-semibold px-8 py-4 rounded-lg hover:-translate-y-1 hover:shadow-cta-glow transition-all duration-200 cursor-pointer`.
- Do NOT include a secondary "Learn About Us" button (links to deleted /about page).
- Do NOT include blur orbs or background effects.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/home/TestimonialsSection.tsx src/components/home/CtaBanner.tsx 2>&1 || echo "TypeScript check done"</automated>
  </verify>
  <done>TestimonialsSection.tsx exports TestimonialsSection with all 3 testimonials using EXACT verbatim quote text from REQUIREMENTS.md. Jesse Batt has role "Owner of Performance Meal Prep". Kerry Johnson and Daniel have no role rendered. CtaBanner.tsx exports CtaBanner with heading and #booking CTA link. Both are Server Components. No hardcoded hex. No links to deleted pages.</done>
</task>

</tasks>

<verification>
- All 4 files exist in src/components/home/
- No file contains "use client"
- No file contains hardcoded hex colors (no `#333`, `#fff`, `bg-[#`, `text-[#` patterns)
- No file links to /services, /about, /contact
- ServicesSection has exactly 3 service items
- TestimonialsSection has exactly 3 testimonials with verbatim text
- HeroSection and CtaBanner both have href="#booking"
</verification>

<success_criteria>
Four Server Component files are created and TypeScript-clean. They implement all 9 Phase 2 requirements (HERO-01 through CTA-01) using dark palette tokens from Phase 1. No client-side JavaScript. Ready for composition in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-01-SUMMARY.md`
</output>
